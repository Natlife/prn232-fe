@model CarSalesManagementSystemClient.Models.ResetPasswordViewModel

@{
    ViewData["Title"] = "Đặt lại mật khẩu";
}

<div class="row justify-content-center mt-5">
    <div class="col-md-5">
        <div class="card shadow-lg border-0 rounded-lg">
            <div class="card-header bg-info text-white text-center">
                <h3 class="font-weight-light my-2">Đặt lại mật khẩu mới</h3>
            </div>
            <div class="card-body">
                <p class="text-muted text-center">Nhập mã OTP đã được gửi vào Email <b>@Model.Email</b> cùng với mật khẩu mới.</p>
                
                @if (ViewBag.Error != null)
                {
                    <div class="alert alert-danger">@ViewBag.Error</div>
                }
                @if (TempData["Success"] != null)
                {
                    <div class="alert alert-success">@TempData["Success"]</div>
                }

                <form asp-action="ResetPassword" method="post">
                    <input type="hidden" asp-for="Email" />
                    
                    <div class="form-group mb-3">
                        <label class="small mb-1" asp-for="Otp">Mã OTP (6 số)</label>
                        <input class="form-control" asp-for="Otp" placeholder="Nhập mã OTP" maxlength="6" />
                        <span asp-validation-for="Otp" class="text-danger"></span>
                    </div>
                    
                    <div class="form-group mb-3">
                        <label class="small mb-1" asp-for="NewPassword">Mật khẩu mới</label>
                        <input class="form-control" asp-for="NewPassword" placeholder="Nhập mật khẩu mới" />
                        <span asp-validation-for="NewPassword" class="text-danger"></span>
                    </div>

                    <div class="form-group mt-4 mb-0 text-center">
                        <button type="submit" class="btn btn-info text-white w-100">Đổi mật khẩu</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
